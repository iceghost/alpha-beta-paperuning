import { MinimaxNode } from '$lib/minimax';
import { Component } from 'solid-js';

const MinimaxTree: Component<{ root: MinimaxNode }> = (props) => {
  return <p>Hello world</p>;
};

export default MinimaxTree;
